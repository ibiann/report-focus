/* var str = 'học kém quá trời ơi';

console.log(str);

function myFunction() {
  var content = '';
  if (str.startsWith('học kém'), 1) {
    content = 'chuỗi học bắt đầu';
  } else {
    content = 'chuỗi học chưa bắt đầu';
  }

  console.log(content);
}  */

//str.startsWith()

/* let str = 'Đây là tiếng nói Việt Nam, phát thanh từ Hà Nội, thủ đô nước Cộng hòa Xã hội chủ nghĩa Việt Nam.'

let result = str.startsWith('đây') || str.startsWith('Đây');

console.log(result); */


//str.includes()

/* let str = "Study the JS";
let checkStr = str.includes("Study", 0);


function myFunction() {
    console.log(checkStr);
} */

//str.endsWith() như start ngược lại từ cuối lên

// template

//back-tick

/* const message = `this is a long message that will "span" across multiple lines in the code.`;

console.log(message); */

// nội suy (interpolation)

// const newMessage = `this is a long message that will span across the program`;

// const addMessage = `Add this: ${newMessage}`;

// console.log(addMessage);

/* var math = "Toan";

console.log(math);

let result = `Tong cac so: ${4 + 5}`;

console.log(result);

console.log(`${result < 10 ? 'too low': 'too high'}`); */